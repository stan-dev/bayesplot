#' @param group A grouping variable. Will be coerced to [factor][base::factor]
#'   if not already a factor. Each value in `group` is interpreted as the group
#'   level pertaining to the corresponding observation.
