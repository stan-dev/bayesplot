#' @param pars An optional character vector of parameter names. If neither
#'   `pars` nor `regex_pars` is specified then the default is to use _all_
#'   parameters. As of version `1.7.0`, __bayesplot__ also supports 'tidy'
#'   parameter selection by specifying `pars = vars(...)`, where `...` is
#'   specified the same way as in [dplyr::select][dplyr::select(...)] and
#'   similar functions. Examples of using `pars` in this way can be found on the
#'   [Tidy parameter selection][tidy-params] page.
#' @md
