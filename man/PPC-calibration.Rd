% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ppc-calibration.R
\name{PPC-calibration}
\alias{PPC-calibration}
\alias{ppc_calibration_overlay}
\alias{ppc_calibration_overlay_grouped}
\alias{ppc_calibration}
\alias{ppc_calibration_grouped}
\alias{ppc_loo_calibration}
\alias{ppc_loo_calibration_grouped}
\alias{ppc_calibration_data}
\title{PPC calibration}
\usage{
ppc_calibration_overlay(
  y,
  prep,
  ...,
  prob = NULL,
  linewidth = 0.25,
  alpha = 0.2
)

ppc_calibration_overlay_grouped(
  y,
  prep,
  group,
  ...,
  prob = NULL,
  linewidth = 0.25,
  alpha = 0.2
)

ppc_calibration(
  y,
  prep = NULL,
  yrep = NULL,
  prob = 0.95,
  interval = c("confidence", "consistency"),
  help_text = TRUE,
  B = 200,
  show_mean = TRUE,
  show_qdots = TRUE,
  qdots_quantiles = 100,
  ...,
  linewidth = 1,
  alpha = 0.1
)

ppc_calibration_grouped(
  y,
  yrep = NULL,
  prep = NULL,
  group,
  prob = 0.95,
  interval = c("confidence", "consistency"),
  help_text = TRUE,
  B = 200,
  show_mean = TRUE,
  show_qdots = TRUE,
  qdots_quantiles = 100,
  ...,
  linewidth = 1,
  alpha = 0.1
)

ppc_loo_calibration(
  y,
  yrep,
  lw = NULL,
  psis_object = NULL,
  prob = 0.95,
  interval = c("confidence", "consistency"),
  help_text = TRUE,
  B = 200,
  show_mean = TRUE,
  show_qdots = TRUE,
  qdots_quantiles = 100,
  ...,
  linewidth = 1,
  alpha = 0.1
)

ppc_loo_calibration_grouped(
  y,
  yrep,
  lw = NULL,
  psis_object = NULL,
  group,
  prob = 0.95,
  interval = c("confidence", "consistency"),
  help_text = TRUE,
  B = 200,
  show_mean = TRUE,
  show_qdots = TRUE,
  qdots_quantiles = 100,
  ...,
  linewidth = 1,
  alpha = 0.1
)

ppc_calibration_data(
  y,
  prep = NULL,
  yrep = NULL,
  group = NULL,
  type = c("overlay", "interval"),
  prob = 0.95,
  interval = c("confidence", "consistency"),
  B = 200
)
}
\arguments{
\item{y}{A vector of observations. See \strong{Details}.}

\item{prep}{For \code{ppc_calibration()}, \code{ppc_calibration_grouped()},
\code{ppc_calibration_overlay()}, and \code{ppc_calibration_overlay_grouped()},
an \code{S} by \code{N} matrix of predicted probabilities in \verb{[0, 1]}, where \code{S} is
the number of draws and \code{N} the number of observations (\code{N = length(y)}).}

\item{...}{Currently unused.}

\item{prob}{Probability used to compute the uncertainty intervals. Is \code{NULL}
for \code{ppc_calibration_overlay()} and \code{ppc_calibration_overlay_grouped()},
where no intervals are computed, and defaults to \code{0.95} for the other
calibration functions.}

\item{linewidth, alpha}{Arguments passed to geoms controlling line width and
opacity.}

\item{group}{A grouping variable of the same length as \code{y}.
Will be coerced to \link[base:factor]{factor} if not already a factor.
Each value in \code{group} is interpreted as the group level pertaining
to the corresponding observation.}

\item{yrep}{An \code{S} by \code{N} matrix of draws from the posterior (or prior)
predictive distribution, or a \code{\link[posterior:draws]{posterior::draws}} object. The number of
rows, \code{S}, is the size of the posterior (or prior) sample used to generate
\code{yrep}. The number of columns, \code{N} is the number of predicted observations
(\code{length(y)}). The columns of \code{yrep} should be in the same order as the
data points in \code{y} for the plots to make sense. See the \strong{Details} and
\strong{Plot Descriptions} sections for additional advice specific to particular
plots.}

\item{interval}{For \code{ppc_calibration()}, \code{ppc_calibration_grouped()},
\code{ppc_loo_calibration()}, and \code{ppc_loo_calibration_grouped()}, pointwise
uncertainty interval around the calibration curve. Choose \code{"confidence"}
(default) to answer the question: "Where does the calibration curve of the
model lie?" or \code{"consistency"} to answer the question: "If the model is
correctly specified, where would we expect the calibration curve to fall?".}

\item{help_text}{For \code{ppc_calibration()}, \code{ppc_calibration_grouped()},
\code{ppc_loo_calibration()}, and \code{ppc_loo_calibration_grouped()}, if \code{TRUE}
(default) display a label in the plot indicating the interval type as
\code{CI} (confidence) or \code{CsI} (consistency) with the selected \code{prob}.}

\item{B}{For \code{ppc_calibration()}, \code{ppc_calibration_grouped()},
\code{ppc_loo_calibration()}, and \code{ppc_loo_calibration_grouped()} that use
\code{yrep} with \code{interval = "confidence"}, the number of bootstrap samples.
Default is \code{200}. Ignored if \code{prep} is used or \code{interval = "consistency"}.}

\item{show_mean}{For \code{ppc_calibration()}, \code{ppc_calibration_grouped()},
\code{ppc_loo_calibration()}, and \code{ppc_loo_calibration_grouped()}, if \code{TRUE}
(default), draw the estimated calibration curve.}

\item{show_qdots}{For \code{ppc_calibration()}, \code{ppc_calibration_grouped()},
\code{ppc_loo_calibration()}, and \code{ppc_loo_calibration_grouped()}, if \code{TRUE}
(default) add a quantile dot plot at the bottom of the panel to show the
marginal distribution of predicted probabilities.}

\item{qdots_quantiles}{For \code{ppc_calibration()}, \code{ppc_calibration_grouped()},
\code{ppc_loo_calibration()}, and \code{ppc_loo_calibration_grouped()}, positive
integer indicating the number of dots in the quantile dot plot. Default is
\code{100}.}

\item{lw}{For \code{ppc_loo_calibration()} and \code{ppc_loo_calibration_grouped()},
a matrix of log weights with the same dimensions as \code{yrep}. Either
\code{psis_object} or \code{lw} has to be specified.}

\item{psis_object}{For \code{ppc_loo_calibration()} and \code{ppc_loo_calibration_grouped()},
an object of class \code{"psis"} that is created when the \code{loo()} function calls
\code{psis()} internally to do the PSIS procedure. Either \code{psis_object} or \code{lw}
has to be specified.}

\item{type}{For \code{ppc_calibration_data()}, the data structure to compute:
\code{"overlay"} for \code{ppc_calibration_overlay()} or \code{"interval"} for
\code{ppc_calibration()} and their corresponding _grouped and _loo variants.}
}
\value{
The plotting functions return a ggplot object that can be further
customized using the \strong{ggplot2} package. The functions with suffix
\verb{_data()} return the data that would have been drawn by the plotting
function.
}
\description{
Assess the calibration of the predictions, or predictive probabilites in relation to
binary observations.
See the \strong{Plot Descriptions} section, below, for details.
}
\details{
The ppc_calibration functions are designed to assess the calibration of a
model with binary outcomes. In this context, calibration refers to the
agreement between predicted probabilities and conditional event probabilities
(CEPs) see Dimitriadis et al. (2021) and Säilynoja et al. (2025) for details.

The required inputs are \code{y}, representing binary observations
(0 or 1), and either \code{yrep} or \code{prep}. Specifically,
\code{ppc_calibration_overlay()} and \code{ppc_calibration_overlay_grouped()} require
\code{prep}, while \code{ppc_calibration()}, \code{ppc_calibration_grouped()},
\code{ppc_loo_calibration()}, and \code{ppc_loo_calibration_grouped()} accept either
\code{prep} or \code{yrep}.

A vignette with detailed explanations and examples is available at \url{TODO:add link to vignette}.
}
\section{Plot Descriptions}{

\describe{
\item{\code{ppc_calibration()},\code{ppc_calibration_grouped()}}{
PAV-adjusted calibration plots showing the relationship between the
predicted event probabilities and the conditional event probabilities.
The \code{interval} parameter controls whether confidence intervals, or
consistency intervals are computed around the calibration curve.}
\item{\code{ppc_calibration_overlay()},\code{ppc_calibration_overlay_grouped()}}{
Overlay plots showing posterior samples of PAV-adjusted calibration
curves for each posterior draw, which can be used to visually assess the
uncertainty in the calibration curve.
}
\item{\code{ppc_loo_calibration()},\code{ppc_loo_calibration_grouped()}}{
PAV-adjusted calibration plots to assess the calibration of the
leave-one-out (LOO) predictive probabilities, computed by resampling each
observation's posterior predictive draws using LOO importance weights.
}
\item{\code{ppc_calibration_data()}}{
Data frame containing the data underlying the calibration plots, which can
be used to build custom calibration plots. The \code{type} argument controls
whether the data frame for \code{ppc_calibration_overlay()} and its \verb{_grouped`` variant is computed (}type = "overlay"\verb{), or the data frame for  }ppc_calibration()\verb{and its}_grouped\code{or}_loo\verb{ variant is computed (}type = "interval"`).
}
}
}

\examples{
color_scheme_set("brightblue")

# Make an example dataset of binary observations
ymin <- range(example_y_data(), example_yrep_draws())[1]
ymax <- range(example_y_data(), example_yrep_draws())[2]
y <- rbinom(length(example_y_data()), 1, (example_y_data() - ymin) / (ymax - ymin))
prep <- (example_yrep_draws() - ymin) / (ymax - ymin)

ppc_calibration_overlay(y, prep[1:50, ])

# Compare confidence vs consistency intervals
ppc_calibration(y, prep, interval = "confidence")
ppc_calibration(y, prep, interval = "consistency")
}
\references{
Dimitriadis, T., Gneiting, T., & Jordan, A. I. (2021). Stable
reliability diagrams for probabilistic classifiers. Proceedings of the
National Academy of Sciences, 118(8).
https://doi.org/10.1073/pnas.2016191118

Säilynoja, T., Johnson, A. R., Martin, O. A., & Vehtari, A. (2025).
Recommendations for visual predictive checks in Bayesian workflow.
(Preprint). arXiv. https://doi.org/10.48550/arXiv.2503.01509
}
\seealso{
Other PPCs: 
\code{\link{PPC-censoring}},
\code{\link{PPC-discrete}},
\code{\link{PPC-distributions}},
\code{\link{PPC-errors}},
\code{\link{PPC-intervals}},
\code{\link{PPC-loo}},
\code{\link{PPC-overview}},
\code{\link{PPC-scatterplots}},
\code{\link{PPC-test-statistics}}
}
\concept{PPCs}
